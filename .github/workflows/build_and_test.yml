name: Build and Test

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # validate-packages (icanbwell/actions' validate-pip-packages.yaml reusable workflow) was
  # removed from here: this repo is now public (temporary Render demo), and a public repo
  # cannot call a reusable workflow hosted in a private repo (icanbwell/actions is private;
  # GitHub's org-wide reusable-workflow access setting only extends to other private/internal
  # repos in the org). See icanbwell/bwell-cms-hte-patient-matching-service/README.md for why
  # this wasn't moved there either (that repo's own checkout can't validate this repo's
  # dependencies without reproducing a dependency-overlay setup disproportionate to a
  # temporary public state) -- this is a documented gap, not a fix.
  build_and_test:
    # Back on ubuntu-latest (GitHub-hosted): this repo going public broke self-hosted-runner
    # access (GitHub gates self-hosted runner groups against public repos by default, so
    # `runs-on: main` jobs here sat queued forever -- confirmed on this branch's own CI runs).
    # The only reason this job was ever moved to self-hosted `main` was to pull
    # root-mirror/python (needs AWS/ECR auth) for the pre-commit/up steps below. Dockerfile
    # and pre-commit.Dockerfile now build from public.ecr.aws instead, so that requirement is
    # gone. The Root.io-hardened build now lives in bwell.Dockerfile in
    # icanbwell/bwell-cms-hte-patient-matching-service (the repo with ECR access), matching
    # icanbwell/kill-the-clipboard-scanner's own public/private split -- see that repo's
    # README.md for the rationale (a literal root-mirror FROM in a public-facing repo also
    # scores as a critical compliance finding, independent of the CI issue).
    runs-on: ubuntu-latest
    # JFrog creds (apk repos + uv sync inside the Dockerfiles) are unrelated to the AWS/ECR
    # access above -- plain GH secrets, no self-hosted runner needed for these.
    env:
      JFROG_READ_USER: ${{ secrets.JFROG_READ_USER }}
      JFROG_READ_TOKEN: ${{ secrets.JFROG_READ_TOKEN }}

    steps:
      # Checks-out your repository
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: create .env
        run: |
          touch ./.env

      - name: pre-commit
        run: make run-pre-commit && make clean-pre-commit

      - name: Set correct permissions for tests
        run: |
          sudo chmod -R 777 ${{ github.workspace }}

      - name: Create reports output folder
        run: |
          mkdir -p ${{ github.workspace }}/reports
          chmod -R 777 ${{ github.workspace }}/reports


      # setup and install everything and run tests
      - name: up
        run: make up

      - name: tests
        run: |
          docker compose run --rm --name patient-matching-service-tests -v ${{ github.workspace }}/reports:/reports dev pytest . --tb=auto --junitxml=/reports/test-results.xml

      - name: Upload pytest test results
        uses: actions/upload-artifact@v7
        with:
          name: pytest-results
          path: ${{ github.workspace }}/reports/**/*.xml
        # Use always() to always run this step to publish test results when there are test failures
        if: ${{ always() }}

      - name: Verify reports are generated
        if: ${{ always() }}
        run: ls -halt ${{ github.workspace }}/reports/

      # Publish all test results in the GitHub UI
      - name: Surface failing tests
        if: always()
        uses: pmeier/pytest-results-action@fdc7f18d9934e38aca411ca9557e6577bd25ca9c # https://github.com/pmeier/pytest-results-action
        with:
          # A list of JUnit XML files, directories containing the former, and wildcard
          # patterns to process.
          # See @actions/glob for supported patterns.
          path: ${{ github.workspace }}/reports/*.xml

          # (Optional) Add a summary of the results at the top of the report
          summary: true

          # (Optional) Select which results should be included in the report.
          # Follows the same syntax as `pytest -r`
          display-options: fEX

          # (Optional) Fail the workflow if no JUnit XML was found.
          fail-on-empty: true

          # (Optional) Title of the test results section in the workflow summary
          title: Test results

      - name: Cleanup
        if: always()
        run: |
          docker compose down -v --remove-orphans

      - name: 'Cleanup build folder'
        if: always()
        run: |
          ls -la ./
          sudo rm -rf ${{ github.workspace }}/*
          ls -la ./
